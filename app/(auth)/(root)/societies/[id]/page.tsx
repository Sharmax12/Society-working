import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, ArrowUpRight, CalendarDays, MessageCircle, Sparkles, Users, CheckCircle2 } from "lucide-react";
import { auth } from "@/auth";
import { getExistingApplication, getSocietyWithQuestions } from "@/modules/societies/queries";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";

type Props = { params: Promise<{ id: string }> };
export async function generateMetadata({ params }: Props): Promise<Metadata> { const { id } = await params; const society = await getSocietyWithQuestions(id); if (!society) return { title: "Society not found" }; return { title: society.name, description: society.description.slice(0,155) }; }

export default async function SocietyDetailPage({ params }: Props) {
  const { id } = await params; const society = await getSocietyWithQuestions(id); if (!society) notFound();
  const session = await auth(); const alreadyApplied = session?.user?.id ? Boolean(await getExistingApplication(session.user.id, society.id)) : false;
  const deadline = new Date(society.deadline);
  return <main className="min-h-screen"><section className="relative overflow-hidden border-b border-foreground/10 surface-grid"><div className="absolute right-0 top-0 h-96 w-96 rounded-full bg-primary/10 blur-3xl"/><div className="relative mx-auto max-w-7xl px-5 pb-16 pt-10 sm:px-8 lg:px-10"><Link href="/societies" className="inline-flex items-center gap-2 text-xs font-bold text-muted-foreground transition hover:text-foreground"><ArrowLeft className="h-4 w-4"/> All societies</Link><div className="mt-12 grid gap-10 lg:grid-cols-[1fr_390px] lg:items-end"><div><div className="mb-5 flex items-center gap-2 text-[11px] font-black uppercase tracking-[.2em] text-primary"><Sparkles className="h-4 w-4"/> Society profile</div>{society.category && <Badge variant="secondary" className="mb-4 rounded-full capitalize">{society.category}</Badge>}<h1 className="text-balance text-5xl font-black tracking-[-.06em] sm:text-7xl">{society.name}</h1><p className="mt-6 max-w-2xl text-base leading-7 text-muted-foreground sm:text-lg">{society.description}</p></div><div className="rounded-[1.7rem] border border-foreground/10 bg-background/80 p-6 shadow-2xl backdrop-blur-xl"><div className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-primary/10 text-primary"><Users className="h-5 w-5"/></div><div><p className="font-black">Join the community</p><p className="text-xs text-muted-foreground">Find your people and make something happen.</p></div></div><div className="mt-6 flex items-center gap-2 rounded-xl bg-muted/70 p-3 text-xs text-muted-foreground"><CalendarDays className="h-4 w-4 text-primary"/> Applications close {deadline.toLocaleDateString("en-US",{month:"long",day:"numeric",year:"numeric"})}</div><div className="mt-4">{alreadyApplied ? <Button disabled className="h-12 w-full rounded-xl"><CheckCircle2 className="mr-2 h-4 w-4"/> Application submitted</Button> : society.isOpen ? <Link href={`/apply/${society.id}`}><Button variant="brand" size="lg" className="h-12 w-full rounded-xl">Apply to join <ArrowUpRight className="ml-2 h-4 w-4"/></Button></Link> : <Button disabled variant="outline" className="h-12 w-full rounded-xl">Applications closed</Button>}</div></div></div></div></section><div className="mx-auto max-w-7xl px-5 py-12 sm:px-8 lg:px-10"><div className="grid gap-4 md:grid-cols-2"><div className="rounded-[1.7rem] border border-foreground/10 bg-card p-7"><MessageCircle className="mb-8 h-5 w-5 text-primary"/><h2 className="text-xl font-black">Community conversations</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Society chat is coming soon — a dedicated space for members to share ideas, coordinate and stay connected.</p><Badge variant="secondary" className="mt-5 rounded-full">Coming soon</Badge></div><div className="rounded-[1.7rem] border border-foreground/10 bg-card p-7"><Users className="mb-8 h-5 w-5 text-orange-500"/><h2 className="text-xl font-black">A place to belong</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Understand the society, explore the application process and take your first step into campus life.</p></div></div></div></main>;
}
