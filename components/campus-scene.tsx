// Kept separate so the homepage background can evolve without touching the page.
export default function CampusScene() {
  return (
    <div className="absolute inset-0 -z-10 overflow-hidden pointer-events-none" />
  );
}
